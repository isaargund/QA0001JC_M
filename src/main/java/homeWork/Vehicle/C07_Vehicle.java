package homeWork.Vehicle;

public interface C07_Vehicle {

    //1- Create an interface called "Vehicle" with the following methods: "start" and "stop".
// 2-Create a class called "Car" that implements the "Vehicle" interface and provides implementations
// for the "start" and "stop" methods.
// 3-In the "start" method, print a message indicating that "the car has started",
// and in the "stop" method, print a message indicating that "the car has stopped".
//4- Create another class called "Bicycle" that implements the "Vehicle" interface
// and provides implementations for the "start" and "stop" methods.
// In the "start" method, print a message indicating that "the bicycle has started",
// and in the "stop" method, print a message indicating that "the bicycle has stopped".
// In the main method, create instances of the "Car" and "Bicycle" classes, and call their "start" and "stop" methods.

void  start();
void stop();
}
